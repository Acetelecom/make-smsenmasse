{
  "url": "/sms",
  "method": "GET",
  "qs": {
    "page":  "{{parameters.page}}",
    "limit": "{{parameters.limit}}"
  },
  "response": {
    "valid": "{{statusCode === 200}}",
    "output": "{{body}}",
    "error": {
      "message": "[{{statusCode}}] {{ifempty(body.message; 'Error listing campaigns.')}}"
    }
  }
}
