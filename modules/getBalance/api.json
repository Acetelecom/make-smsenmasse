{
  "url": "/sms/balance",
  "method": "GET",
  "response": {
    "valid": "{{statusCode === 200}}",
    "output": "{{body}}",
    "error": {
      "message": "[{{statusCode}}] {{ifempty(body.message; 'Error retrieving balance.')}}"
    }
  }
}
